jdk:
  - openjdk17
before_install:
  - echo "Using Java 17 for Android build"
install:
  - ./gradlew clean :core:assembleRelease :core:publishToMavenLocal :api:assembleRelease :api:publishToMavenLocal
